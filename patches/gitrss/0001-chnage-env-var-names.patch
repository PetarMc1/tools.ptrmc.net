From 892ad5323857dbb2399c5969ea7ad6575480f2c6 Mon Sep 17 00:00:00 2001
From: PetarMc1 <160483025+PetarMc1@users.noreply.github.com>
Date: Mon, 18 May 2026 19:23:16 +0000
Subject: [PATCH 1/3] chnage env var names

---
 backend/src/config/env.ts | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)

diff --git a/backend/src/config/env.ts b/backend/src/config/env.ts
index fc0e713..6be98fa 100644
--- a/backend/src/config/env.ts
+++ b/backend/src/config/env.ts
@@ -49,21 +49,21 @@ function parseAdminPassword(rawAdminPassword: string | undefined): string | unde
 }
 
 export function getServerPort(): number {
-  return parsePort(process.env.PORT);
+  return parsePort(process.env.GITRSS_PORT);
 }
 
 export function getGithubToken(): string | undefined {
-  return parseGithubToken(process.env.GITHUB_TOKEN);
+  return parseGithubToken(process.env.GITRSS_GITHUB_TOKEN);
 }
 
 export function getRedisUrl(): string | undefined {
-  return parseRedisUrl(process.env.REDIS_URL);
+  return parseRedisUrl(process.env.GITRSS_REDIS_URL);
 }
 
 export function getDeepRefreshDays(): number {
-  return parseDeepRefreshDays(process.env.DEEP_REFRESH_DAYS);
+  return parseDeepRefreshDays(process.env.GITRSS_DEEP_REFRESH_DAYS);
 }
 
 export function getAdminPassword(): string | undefined {
-  return parseAdminPassword(process.env.ADMIN_PASSWORD);
+  return parseAdminPassword(process.env.GITRSS_DMIN_PASSWORD);
 }
-- 
2.43.0

